# Python caches / bytecode
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.pytest_cache/

# Virtual environments
.venv/
venv/
env/

# Claude Code / AI tooling state (not project content)
.claude/
CLAUDE.md

# Raw + converted dataset (large, regenerable via convert_coco.py from Roboflow export)
train/
valid/
test/
yolo_dataset/
yolo_dataset.zip
*.zip

# Old / unused model checkpoints and prototyping runs (see repo audit — not the final RT-DETR model)
yolo11n.pt
yolo26n.pt
runs/detect/train/
runs/detect/train-2/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Local secrets/config (defensive — none currently present)
.env
*.env.local
